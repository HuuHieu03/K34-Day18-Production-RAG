---
version: "1.0.0"
date: "2026-08-18"
type: "progress"
status: "PLANNED"
author: "AI Agent"
target_component: "Lab 18: Production RAG Pipeline"
tags: ["progress", "tracking", "lab18"]
summary: "Theo dõi tiến độ thực thi thực tế của bài Lab 18 qua các giai đoạn kỹ thuật đã được lập kế hoạch."
---

# 📈 Theo Dõi Tiến Độ: Triển Khai Lab 18 - Production RAG Pipeline

## 1. 📌 Tổng Quan Tiến Độ (Progress Overview)

- **Kế hoạch liên kết**: [v1.0.0_2026-08-18_lab18_execution_plan.md](../plans/v1.0.0_2026-08-18_lab18_execution_plan.md)
- **Trạng thái tổng thể**: `PLANNED` (Hoàn thành 0/7 giai đoạn - 0%)
- **Người phụ trách**: AI Agent / User
- **Thời gian bắt đầu**: 2026-08-18
- **Dự kiến hoàn thành**: 2026-08-18

---

## 2. ✅ Danh Sách Công Việc Theo Giai Đoạn (Checklist Tasks)

### Giai Đoạn 0: Setup & Baseline
- [ ] Dùng `uv` khởi tạo môi trường `.venv`.
- [ ] Cài đặt các thư viện từ `requirements.txt` bằng `uv`.
- [ ] Cấu hình `.env` với các API keys cần thiết.
- [ ] Chạy lệnh `docker compose up -d` để khởi động Qdrant.
- [ ] Tải trước các models sentence-transformers để tránh lỗi timeout.
- [ ] Chạy thử `uv run python naive_baseline.py` để ghi nhận báo cáo `naive_baseline_report.json`.

### Giai Đoạn 1: Module 1 - Advanced Chunking
- [ ] Thảo luận logic 3 thuật toán chia văn bản.
- [ ] Lập trình hoàn thiện tệp `src/m1_chunking.py`.
- [ ] Chạy thành công lệnh `uv run pytest tests/test_m1.py -v` (Pass 100%).

### Giai Đoạn 2: Module 2 - Hybrid Search
- [ ] Thảo luận thuật toán phân tách từ Tiếng Việt và công thức RRF.
- [ ] Lập trình hoàn thiện tệp `src/m2_search.py`.
- [ ] Chạy thành công lệnh `uv run pytest tests/test_m2.py -v` (Pass 100%).

### Giai Đoạn 3: Module 3 - Reranking
- [ ] Thảo luận luồng hoạt động của CrossEncoder.
- [ ] Lập trình hoàn thiện tệp `src/m3_rerank.py`.
- [ ] Chạy thành công lệnh `uv run pytest tests/test_m3.py -v` (Pass 100%).

### Giai Đoạn 4: Module 4 - RAGAS Evaluation
- [ ] Thảo luận 4 metrics cốt lõi của RAGAS và cơ chế phân tích lỗi.
- [ ] Lập trình hoàn thiện tệp `src/m4_eval.py`.
- [ ] Chạy thành công lệnh `uv run pytest tests/test_m4.py -v` (Pass 100%).

### Giai Đoạn 5: Module 5 - Data Enrichment
- [ ] Thảo luận cách sử dụng LLM để sinh Metadata và Summary từ nội dung đoạn văn.
- [ ] Lập trình hoàn thiện tệp `src/m5_enrichment.py`.
- [ ] Chạy thành công lệnh `uv run pytest tests/test_m5.py -v` (Pass 100%).

### Giai Đoạn 6: Tích hợp, Chạy Pipeline và Báo cáo (Deliverables)
- [ ] Xóa sạch mọi thẻ `# TODO` còn sót lại bằng `grep`.
- [ ] Chạy thành công toàn bộ đường ống `uv run python src/pipeline.py`.
- [ ] Phân tích `ragas_report.json` và cập nhật file `analysis/failure_analysis.md`.
- [ ] Hoàn thiện file `analysis/reflection_[HọTên].md`.
- [ ] Chạy `check_lab.py` để kiểm định định dạng trước khi nộp.
- [ ] Push code lên kho lưu trữ.

---

## 3. ⚠️ Ghi Chú & Trở Ngại (Notes & Blockers)

### 🚧 Trở Ngại Hiện Tại (Blockers)
- *Hiện chưa có trở ngại nào. Sẵn sàng bắt đầu Giai đoạn 0.*

### 💡 Ghi Chú Kỹ Thuật & Cảnh Báo (Technical Notes)
- Bắt buộc kiểm tra `uv` và `.venv` ở mọi phiên làm việc để tránh lệch môi trường.
- `naive_baseline.py` bắt buộc phải được chạy ở bước đầu tiên. Không được nhảy cóc.
